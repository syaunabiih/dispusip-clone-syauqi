<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Admin - Sistem Referensi</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-gray-50 via-blue-50 to-indigo-50 flex items-center justify-center min-h-screen p-3 sm:p-4 md:p-6 relative">

<!-- Tombol Back -->
<a href="/" 
   class="absolute top-4 left-4 sm:top-6 sm:left-6 bg-white text-gray-700 px-3 sm:px-4 py-2 rounded-lg shadow-md hover:bg-gray-50 hover:shadow-lg transition-all duration-200 flex items-center gap-2 text-sm sm:text-base font-medium z-10">
    <i class="fas fa-arrow-left"></i>
    <span class="hidden sm:inline">Kembali</span>
</a>

<div class="bg-white shadow-2xl rounded-xl sm:rounded-2xl p-5 sm:p-6 md:p-8 w-full max-w-md transform transition-all duration-300">
    <!-- Header -->
    <div class="text-center mb-6 sm:mb-8">
        <h2 class="text-xl sm:text-2xl md:text-3xl font-bold text-gray-800 mb-2 sm:mb-4">
            <i class="fas fa-user-shield text-blue-600 mr-2"></i>Admin Login
        </h2>
        
        <!-- Gambar SmarLib -->
        <div class="flex justify-center mb-4 sm:mb-6">
            <img src="/image/assets/SmarLib.png" alt="SmarLib Logo" 
                 class="w-20 h-20 sm:w-24 sm:h-24 md:w-28 md:h-28 object-contain transition-transform hover:scale-105">
        </div>
    </div>

    <!-- Pesan error -->
    <% if (error) { %>
        <div class="bg-red-50 border-l-4 border-red-500 text-red-700 p-3 sm:p-4 rounded-lg mb-4 sm:mb-6 text-sm sm:text-base">
            <div class="flex items-center">
                <i class="fas fa-exclamation-circle mr-2"></i>
                <span>Username atau password salah</span>
            </div>
        </div>
    <% } %>

    <form action="/admin/login" method="POST" class="space-y-4 sm:space-y-5">
        <div>
            <label class="block text-sm sm:text-base font-medium text-gray-700 mb-1.5 sm:mb-2">
                <i class="fas fa-user text-gray-400 mr-2"></i>Username
            </label>
            <input type="text" name="username" required
                class="w-full px-3 sm:px-4 py-2.5 sm:py-3 text-sm sm:text-base border-2 border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200" 
                placeholder="Masukkan username" />
        </div>

        <div>
            <label class="block text-sm sm:text-base font-medium text-gray-700 mb-1.5 sm:mb-2">
                <i class="fas fa-lock text-gray-400 mr-2"></i>Password
            </label>
            <div class="relative">
                <input type="password" id="password" name="password" required
                    class="w-full px-3 sm:px-4 py-2.5 sm:py-3 pr-10 sm:pr-12 text-sm sm:text-base border-2 border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200" 
                    placeholder="Masukkan password" />

                <!-- Tombol Show/Hide dengan icon -->
                <button type="button" onclick="togglePassword()"
                        class="absolute inset-y-0 right-2 sm:right-3 flex items-center px-2 sm:px-3 text-gray-500 hover:text-gray-700 focus:outline-none transition-colors">
                    <i id="eyeIcon" class="fas fa-eye text-lg sm:text-xl"></i>
                </button>
            </div>
        </div>

        <button type="submit"
                class="w-full py-2.5 sm:py-3 bg-gradient-to-r from-blue-500 to-indigo-600 text-white font-semibold text-sm sm:text-base rounded-lg shadow-md hover:from-blue-600 hover:to-indigo-700 active:scale-95 transition-all duration-200 flex items-center justify-center">
            <i class="fas fa-sign-in-alt mr-2"></i>
            <span>Login</span>
        </button>
    </form>

    <p class="text-xs sm:text-sm text-gray-500 text-center mt-4 sm:mt-6 pt-4 sm:pt-6 border-t border-gray-200">
        <i class="fas fa-copyright mr-1"></i>2026 Sistem Referensi
    </p>
</div>

<script>
function togglePassword() {
    const input = document.getElementById('password');
    const icon = document.getElementById('eyeIcon');
    
    if (input.type === 'password') {
        input.type = 'text';
        icon.classList.remove('fa-eye');
        icon.classList.add('fa-eye-slash');
    } else {
        input.type = 'password';
        icon.classList.remove('fa-eye-slash');
        icon.classList.add('fa-eye');
    }
}
</script>

</body>
</html>