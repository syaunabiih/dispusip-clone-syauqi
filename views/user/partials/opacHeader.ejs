<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Sistem Referensi</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>

<body class="bg-slate-50 min-h-screen flex flex-col font-sans">

<!-- Navbar -->
<nav class="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm w-full">
    <div class="w-full px-6 h-16 flex justify-between items-center">
        
        <% 
            let homeLink = '/';
            // Cek variable query dari controller (biasanya ada di searchPage dan detail controller)
            if (typeof query !== 'undefined' && query.id_ruangan) {
                homeLink = `/books?id_ruangan=${query.id_ruangan}`;
            }
        %>

        <div class="flex items-center cursor-pointer group" onclick="window.location='<%= homeLink %>'">
            <img src="/image/assets/smarLIB.png" class="h-12 mr-3 group-hover:opacity-90 transition">
            <div>
                <h1 class="text-lg font-bold text-gray-900 group-hover:text-blue-700 transition">Sistem Unit Layanan</h1>
                <p class="text-xs text-gray-500">Dispusip Provinsi Sumatera Barat</p>
            </div>
        </div>

        <div>
            <a href="/admin/login" class="text-gray-500 hover:text-blue-600 text-sm font-medium flex items-center gap-2 px-3 py-2 rounded-md hover:bg-blue-50 transition duration-200">
                <i class="fas fa-user-shield"></i>
                <span class="hidden sm:inline">Login Admin</span>
            </a>
        </div>

    </div>
</nav>

<main class="flex-grow w-full px-6 py-4">