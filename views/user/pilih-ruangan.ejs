<%- include('partials/header') %>

<div class="min-h-[calc(100vh-200px)] flex flex-col items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
    
    <div class="text-center max-w-2xl mx-auto mb-12">
        <h1 class="text-3xl md:text-4xl font-extrabold text-gray-800 tracking-tight mb-3">
            Pilih Lokasi Koleksi
        </h1>
        <p class="text-gray-500 text-lg">
            Silakan pilih ruangan di bawah ini untuk menelusuri katalog buku yang tersedia secara spesifik.
        </p>
    </div>

    <div class="w-full max-w-7xl">
        <% if (daftarRuangan && daftarRuangan.length > 0) { %>
            <div class="flex flex-wrap justify-center gap-6 sm:gap-8">
                <% daftarRuangan.forEach(ruang => { %>
                    <a href="/books?id_ruangan=<%= ruang.id_ruangan %>" 
                       class="group relative bg-white rounded-2xl p-6 shadow-sm border border-gray-100 hover:shadow-xl hover:border-blue-300 transition-all duration-300 ease-in-out transform hover:-translate-y-1 flex flex-col items-center text-center w-full sm:w-72 md:w-80 overflow-hidden">
                        
                        <div class="absolute inset-0 bg-blue-50 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>

                        <div class="relative z-10 w-16 h-16 mb-4 rounded-full bg-blue-100 text-blue-600 flex items-center justify-center group-hover:bg-blue-600 group-hover:text-white transition-colors duration-300 shadow-sm">
                            <i class="fas fa-book-reader text-2xl"></i>
                        </div>

                        <div class="relative z-10 w-full">
                            <h2 class="text-xl font-bold text-gray-800 group-hover:text-blue-700 transition-colors mb-2 truncate px-2">
                                <%= ruang.nama_ruangan %>
                            </h2>
                            <p class="text-sm text-gray-500 group-hover:text-gray-600">
                                Klik untuk melihat koleksi
                            </p>
                        </div>

                        <div class="relative z-10 mt-4 opacity-0 group-hover:opacity-100 transform translate-y-2 group-hover:translate-y-0 transition-all duration-300">
                            <span class="text-blue-600 text-sm font-semibold flex items-center gap-2">
                                Masuk <i class="fas fa-arrow-right"></i>
                            </span>
                        </div>
                    </a>
                <% }) %>
            </div>
        <% } else { %>
            <div class="text-center py-12 bg-white rounded-xl shadow-sm border border-gray-200 max-w-lg mx-auto">
                <div class="text-gray-300 mb-4">
                    <i class="fas fa-folder-open text-6xl"></i>
                </div>
                <h3 class="text-lg font-medium text-gray-900">Belum ada data ruangan</h3>
                <p class="text-gray-500 mt-1 px-4">Silakan hubungi administrator untuk menambahkan ruangan.</p>
            </div>
        <% } %>
    </div>

    <div class="mt-12">
        <a href="/" class="inline-flex items-center gap-2 text-gray-600 hover:text-blue-600 font-medium transition-colors px-6 py-3 rounded-full hover:bg-white hover:shadow-md border border-transparent hover:border-gray-100">
            <i class="fas fa-home"></i> Kembali ke Menu Utama
        </a>
    </div>

</div>

<%- include('partials/footer') %>